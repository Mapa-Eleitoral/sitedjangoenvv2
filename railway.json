{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd siteDjangoProject && pip install -r ../requirements.txt && python manage.py collectstatic --noinput && python manage.py migrate"
  },
  "deploy": {
    "startCommand": "cd siteDjangoProject && gunicorn siteDjango.wsgi:application --bind 0.0.0.0:$PORT --workers 1 --threads 2 --timeout 120"
  }
}