{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "gunicorn --chdir siteDjangoProject --workers 1 --threads 2 --timeout 120 siteDjango.wsgi:application --bind 0.0.0.0:$PORT"
  }
}